# Proxies

Cross-platform scripts + configs for running a local **Anthropic-compatible** endpoint backed by other providers (via LiteLLM Proxy).

## Layout

- `windows/`: Windows scripts + proxy project
- `mac/`: macOS scripts + proxy project

## Security

- Put secrets in `.env` files (ignored by git). Use `.env.example` templates.

